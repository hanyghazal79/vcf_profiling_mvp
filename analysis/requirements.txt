# Update requirements.txt with these changes:
fastapi==0.104.1
uvicorn==0.24.0
gunicorn==21.2.0
pydantic==2.5.0
asgiref==3.7.2
# Remove a2wsgi - not needed for Render
# a2wsgi==1.10.0

# Data processing - use compatible versions
pandas==2.1.3
numpy==1.24.3

# Visualization
plotly==5.18.0
matplotlib==3.8.2
seaborn==0.13.0

# PDF generation
reportlab==4.0.9
Pillow==10.1.0

# HTTP requests
requests==2.31.0

# Add render-specific requirements
python-multipart==0.0.6
# cyvcf2 won't work on Render free tier, so ensure we don't depend on it